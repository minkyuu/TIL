# 2021.08.05. TIL (OS)

### 멀티 프로그래밍
```
- 여러 응용 프로그램 실행을 가능토록 함
- 응용 프로그램이 동시에 실행되는 것처럼 보이도록 함
- CPU를 쉬지 않고 응용 프로그램을 실행토록 해서, 짧은 시간 안에 응용 프로그램이 실행 완료될 수 있도록 함
- 컴퓨터 응답 시간도 짧게 해서, 다중 사용자도 지원


메모리 계층 (컴퓨터 구조 내용)

< 각 계층 별 접근 시간 >
==== On the datapath ====
- Registers : 1 cycle

==== On chip ====
- Level 1 Cache : 2~4 cycles
- Level 2 Cache : 10 cycles
- Level 3 Cache : 40 cycles (레지스터의 40배 느림)

==== Other chips ====
- Main Memory : 200 cycles
- Flash Drive : 10~100us

==== Mechanical devices ====
- Hard Disk : 10ms (파일을 읽는 시간이 이렇게나 길다  그래서 멀티 프로그래밍이 필요)


System Bus (컴퓨터 구조)
: CPU에서 저장매체로 이동하는데 걸리는 시간도 가장 길다..
-> 중간에 DMA를 만들어 시간을 단축시킴
-> 이 또한, 멀티 프로그래밍의 필요성
```



### 프로세스란 ? (매우 중요)
```
: 메모리에 올려져서 실행 중인 프로그램

응용 프로그램 != 프로세스
: 응용 프로그램은 여러 개의 프로세스로 이루어질 수 있고 프로세스들과 상호작용을 하면서 실행될 수 있다.

IPC 기법
: 여러 프로그램을 만들어서, 프로그램들끼리 서로 통신을 하면서 프로그램을 작성할 수 있도록 하는 기법
```


### 스케줄러
```
: 프로세스를 관리해주는 시스템
```

### 스케줄링 알고리즘
```
- FIFO(FCFS) 스케줄링 알고리즘 (배치 처리 시스템)
- 최단 작업 우선(SJF) 스케줄링 알고리즘
- 우선순위 기반 스케줄링 알고리즘
   정적 우선순위 , 동적 우선순위

- Round Robin 스케줄링 알고리즘
 시분할 시스템 기반!!!!

프로세스 상태
1. 프로세스 생성 (new)
2. 실행 가능 (ready)
 : CPU에서 실행 가능 상태 (실행 대기 상태)
3. 실행 중 (running)
 : 현재 CPU에서 실행 상태
4. 대기 (blocked)
 : 특정 이벤트 발생 대기 상태
5. 종료 (exit)
```

---


※ 프로세스의 상태와 관련하여 CPU를 점유하는 부분에 있어서 관련된 스케줄링 알고리즘에는 크게 두 가지 방식이 있다.


### 선점형 스케줄링  vs  비선점형 스케줄링
```
- 선점형 스케줄링 (Preemptive Scheduling)
 : 하나의 프로세스가 다른 프로세스 대신에 프로세서(CPU)를 차지할 수 있다.
 : 프로세스가 running 상태임에도 스케줄러가 이를 중단시키고 다른 프로세스로 교체가 가능하다.

- 비선점형 스케줄링 (Non – preemptive Scheduling)
 : 하나의 프로세스의 작업이 끝나지 않으면 다른 프로세스는 CPU를 사용할 수 없다.
 : 프로세스가 자발적으로 blocking 상태로 들어가거나, 실행이 끝났을 때만, 다른 프로세스로 교체 가능!  (그 외에는 교체 불가능하다)
```
